<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.metadatacenter</groupId>
    <artifactId>cedar-parent</artifactId>
    <version>2.9.2-SNAPSHOT</version>
  </parent>

  <groupId>org.metadatacenter</groupId>
  <artifactId>cedar-config-library</artifactId>
  <version>2.9.2-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>CEDAR Config Library</name>

  <properties>
  </properties>

  <scm>
    <connection>scm:git:https://github.com/metadatacenter/cedar-config-library.git</connection>
    <developerConnection>scm:git:https://github.com/metadatacenter/cedar-config-library.git</developerConnection>
    <url>https://github.com/metadatacenter/cedar-config-library</url>
  </scm>

  <dependencies>

    <dependency>
      <groupId>org.metadatacenter</groupId>
      <artifactId>cedar-core-library</artifactId>
    </dependency>

    <dependency>
      <groupId>org.metadatacenter</groupId>
      <artifactId>cedar-rest-library</artifactId>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.jackson.datatype</groupId>
      <artifactId>jackson-datatype-jsr310</artifactId>
    </dependency>

    <dependency>
      <groupId>io.dropwizard</groupId>
      <artifactId>dropwizard-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>

      <!--
        This shaded jar bundles the classes of cedar-core-library, cedar-rest-library and
        cedar-model-library alongside its own, so those classes exist in more than one published
        artifact. Consumers resolve whichever copy the classpath reaches first, which is not
        necessarily the one that was changed last.

        The consequence: a change to any library shaded here is invisible until THIS library is
        published again, and the build that fails is one that did not change. Adding a constant to
        cedar-core-library broke cedar-microservice-libraries on develop with "cannot find symbol",
        because the copy inside this jar predated it. Publishing is gated on a real push to develop,
        so a workflow_dispatch rebuild verifies and does not republish.

        So: after changing a library shaded here, push this repository too, before any consumer
        merges. Removing the CEDAR artifacts from the shaded set is tracked on BACKEND-ROADMAP.md
        under the shared-library shading item.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <configuration>
          <createDependencyReducedPom>true</createDependencyReducedPom>
          <filters>
            <filter>
              <artifact>*:*</artifact>
              <excludes>
                <exclude>META-INF/*.SF</exclude>
                <exclude>META-INF/*.DSA</exclude>
                <exclude>META-INF/*.RSA</exclude>
              </excludes>
            </filter>
          </filters>
        </configuration>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-release-plugin</artifactId>
      </plugin>

    </plugins>

  </build>
</project>
